from setuptools import setup, find_namespace_packages

setup(
    name='scatpack_plugin_a',

    version='0.1.0',

    description='This is the plugin A functionality.',
    long_description='Here is a very very very very very long description.',

    author='Jason',
    author_email='jason@spraoi.ai',

    # namespace_packages=['scatpack'],
    packages=['scatpack.plugin_a'],
    # packages=find_namespace_packages(),
)
